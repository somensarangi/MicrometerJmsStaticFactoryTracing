name: CI

on:
  push:
    branches: [ "dev","stg" ]
    tags: [ "v*" ]

  workflow_dispatch:
  
# This is environment variable 1
# env:
#   FILE_NAME: Changelog.MD

jobs:
  build:
    runs-on: ubuntu-latest
    
    # env:
    #   inside: inside_build

    steps:
      - uses: actions/checkout@v4

      #      - name: Call from secret
      #        run: echo ${{ secrets.INITIAL_TOKEN }}
      #
      #      - name: Call from variable
      #        run: echo ${{ vars.INITIAL_VARIABLE }}

      - name: Check ref name
        run: echo $GITHUB_REF_NAME

      # - name: Run a one-line script
      #   run: | 
      #     echo Hello, world! $FILE_NAME
      #     echo $inside
      #     echo $outside

      # - name: Run a multi-line script
      #   run: |
      #     echo Hello, world! $FILE_NAME
      #     echo $inside
      #     echo $outside
      #   env: 
      #     outside: inside_step
    

      # - name: build
      #   if: startsWith(github.ref_name, 'dev') || startsWith(github.ref_name, 'stg') 
      #   run: docker build -t somendocker/webapp:latest -t somendocker/webapp:$GITHUB_REF_NAME -f Dockerfile .
      
      # - name: build_prod
      #   if: startsWith(github.ref_name, 'master')
      #   run: docker build -t somendocker/webapp:$(git ls-remote --tags --sort=committerdate | grep -o 'v.*' | sort -r | head -1) -f Dockerfile .  
      
      # - name: login
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ vars.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: push
      #   if: startsWith(github.ref_name, 'dev') || startsWith(github.ref_name, 'stg')  
      #   run: | 
      #     docker push somendocker/webapp:latest
      #     docker push somendocker/webapp:$GITHUB_REF_NAME

      # - name: push_prod
      #   if: startsWith(github.ref_name, 'master')
      #   run: docker push somendocker/webapp:$(git ls-remote --tags --sort=committerdate | grep -o 'v.*' | sort -r | head -1)
